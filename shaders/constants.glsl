
const float pi  = 3.141592653589793; // tau/2
const float tau = 6.283185307179586; // ratio of a circle's circumfrence to its radius
const float sqrt_2 = sqrt(2.0);
const float sqrt_pi = sqrt(pi);

const vec4 errorColor = vec4(1.0, 0.0, 0.0, 1.0);
